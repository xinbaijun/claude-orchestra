---
description: 中文别名 for /dispatch —— 标准化派单流程
---

请直接执行 `/dispatch` 命令的完整 SOP（见 `.claude/commands/dispatch.md`）。本文件只是中文别名指针，防止双写漂移。

**用法**：`/派单 <任务描述>`

ASCII 是主源（兼容性好）；中文是别名（中文用户偏好）。所有 SOP 更新只改 ASCII 主源文件，本指针文件不动。
