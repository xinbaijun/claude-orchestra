---
description: 中文别名 for /daily —— 跑 work_log 脚本拿原料，加工成工作日志
---

请直接执行 `/daily` 命令的完整 SOP（见 `.claude/commands/daily.md`）。本文件只是中文别名指针，防止双写漂移。

**用法**：`/日报 [today|yesterday|week|<日期>|<起> <止>]`

ASCII 是主源（兼容性好）；中文是别名（中文用户偏好）。所有 SOP 更新只改 ASCII 主源文件，本指针文件不动。
